rabbitmq:
  # Resource overrides for rabbitmq subchart
  resources:
    requests:
      cpu: "0.5"
      memory: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"

minioserver:
  # Resource overrides for minio-server subchart
  resources:
    requests:
      cpu: "0.5"
      memory: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"

pipelinemanager:
  resources:
    requests:
      cpu: "2"
      memory: "4Gi"
    limits:
      cpu: "4"
      memory: "8Gi"
  env:
    USE_VLLM: "CONFIG_ON"
    TICK_INTERVAL_MS: "1000" # EVAM uses TICK (in milliseconds)
    FAST_TICK_INTERVAL_MS: "100" # VLM/LLM uses FAST_TICK (in milliseconds)
    LLM_CONCURRENT: "4"
    VLM_CONCURRENT: "6"

# Disable OVMS when vLLM is selected
ovms:
  enabled: false

vllm:
  enabled: true
  resources:
    requests:
      cpu: "16"
      memory: 128Gi
      ephemeral-storage: 40Gi
    limits:
      cpu: "16"
      memory: 128Gi
      ephemeral-storage: 50Gi

audioanalyzer:
  # Resource overrides for audio-analyzer subchart
  audioanalyzer:
    resources:
      requests:
        cpu: "2"
        memory: "4Gi"
      limits:
        cpu: "6"
        memory: "8Gi"

videoingestion:
  # Resource overrides for video-ingestion subchart
  videoingestion:
    resources:
      requests:
        cpu: "3"
        memory: "6Gi"
      limits:
        cpu: "10"
        memory: "10Gi"

postgresql:
  # Resource overrides for postgresql subchart
  postgresql:
    resources:
      requests:
        cpu: "1"
        memory: "8Gi"
      limits:
        cpu: "2"
        memory: "12Gi"

multimodalembeddingms:
  # Resource overrides for multimodal-embedding-ms subchart
  resources:
    requests:
      cpu: "4"
      memory: "6Gi"
    limits:
      cpu: "8"
      memory: "10Gi"

vdmsdataprep:
  # Resource overrides for vdms-dataprep subchart
  resources:
    requests:
      cpu: "1"
      memory: "6Gi"
    limits:
      cpu: "4"
      memory: "10Gi"

vdmsvectordb:
  # Resource overrides for vdms-vectordb subchart
  resources:
    requests:
      cpu: "2"
      memory: "32Gi"
    limits:
      cpu: "6"
      memory: "48Gi"

videosearch:
  # Resource overrides for video-search subchart
  videosearch:
    resources:
      requests:
        cpu: "1"
        memory: "2Gi"
      limits:
        cpu: "2"
        memory: "4Gi"

videosummaryui:
  image:
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1Gi
